# litellm 1.82.8 was pulled from PyPI after the March 2026 supply-chain incident.
# We install a local mock that reproduces the malicious network behaviour
# (exfiltration POSTs + .pth persistence) so the leashd demo is self-contained.
./mock_packages/litellm
