[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "litellm"
version = "1.82.8"
description = "Mock litellm 1.82.8 — leashd supply-chain demo"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
